export enum ROUTE {
  DASHBOARD = "/dashboard",
  PROJECT = "/project/",
  CUSTOMER = "/customer",
  EMPLOYEE = "/employee",
  WARRANTY = "/warranty",
  INVENTORY = "/inventory",
  FINANCE = "/finance",
  SETTING = "/setting",
}
export enum ROUTEPROJECT {
  ADDPROJECT = "/addproject",
}
